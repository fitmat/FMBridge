using System;
using UnityEngine;

public class FMBridge
{
    private string connectionType = null;


    /*
    * Function:  InitFMBrige
    * --------------------
    * @summary Initiates mat connection using macaddress (BLE)
    * 
    * @param _macaddress    
    *   connect mat using mac address. mac address should be all caps.
    *   
    * @return bool
    *   connection setup success
    */
    public bool InitFMBrige(string _macaddress)
    {

    }


    /*
   * Function:  InitFMBrige
   * --------------------
   * @summary Initiates mat connection using macaddress (BLE)
   * 
   * @param _macaddress    
   *   connect mat using mac address. mac address should be all caps.
   *   
   * @return bool
   *   connection setup success
   */
    public bool InitFMBrige(string _macaddress)
    {

    }


}